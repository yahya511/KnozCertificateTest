# 📚 الدليل الهندسي الشامل والتوثيق البرمجي لمشروع "شهادات أكاديمية كنوز"
**(Comprehensive Engineering Guide & Documentation for Knoz Academy Certificates)**

---

## 📖 الفهرس (Table of Contents)
1. [مقدمة المشروع والأهداف](#1-مقدمة-المشروع-والأهداف)
2. [المعمارية التقنية والتقنيات المستخدمة (Tech Stack)](#2-المعمارية-التقنية-والتقنيات-المستخدمة)
3. [هيكلية المجلدات والملفات (Folder Structure)](#3-هيكلية-المجلدات-والملفات)
4. [إدارة الحالة (State Management)](#4-إدارة-الحالة-باستخدام-signals)
5. [التدفقات البرمجية التفصيلية (Detailed Workflows)](#5-التدفقات-البرمجية-التفصيلية)
   - 5.1. نظام المصادقة والحماية (Auth Flow)
   - 5.2. إنشاء ومعاينة الشهادات (Certificate Flow)
   - 5.3. محرك التصدير لملفات PDF والروابط التفاعلية (PDF Engine)
   - 5.4. نظام التوثيق العام (Verification Dashboard)
6. [نظام تعدد اللغات (i18n & Localization)](#6-نظام-تعدد-اللغات)
7. [الخدمات الأساسية (Core Services)](#7-الخدمات-الأساسية)
8. [واجهات البيانات (Models & Interfaces)](#8-واجهات-البيانات)

---

## 📌 1. مقدمة المشروع والأهداف
تطبيق "شهادات أكاديمية كنوز" هو نظام (Frontend) متقدم مبني لخدمة إدارة إصدار الشهادات الأكاديمية وتوثيقها. يهدف النظام إلى تمكين الإدارة من إنشاء شهادات بتصاميم متعددة (كلاسيكي، أنيق، قرآني)، وتصديرها كملفات PDF عالية الجودة قابلة للطباعة، مع ميزة رائدة وهي تضمين رابط توثيق تفاعلي (QR Code & Clickable Link) داخل ملف الـ PDF. يمكن للطلاب وأولياء الأمور استخدام هذا الرابط لفتح شاشة "توثيق" عامة تؤكد صحة الشهادة وتعرض تفاصيل الكورس باحترافية.

---

## 🛠 2. المعمارية التقنية والتقنيات المستخدمة
تم اختيار حزمة تقنية حديثة جداً لضمان أعلى أداء ممكن:

* **Angular 21 (Zoneless & Standalone):** 
  - **لماذا؟** تم التخلي عن `zone.js` (Zoneless) وعن `NgModules` (Standalone) لجعل التطبيق أسرع في التحميل (Bootstrapping) وتقليل حجم الـ Bundle.
  - تعتمد المكونات على الاستيراد المباشر (Direct Imports) مما يسهل تتبع الكود (Tree-shaking).
* **Tailwind CSS v4:** 
  - إطار عمل لملفات التنسيق يعتمد على الـ Utility Classes.
  - تم استخدامه لبناء واجهات معقدة مثل (Glassmorphism، تأثيرات النبض Animations، تصميم Bento Grid لصفحة التوثيق).
* **Angular Signals:** 
  - تم استخدامه كبديل لـ (RxJS BehaviorSubjects) في إدارة الحالة المحلية والعامة، لأنه يعطي تحديثات لحظية دقيقة (Fine-grained reactivity) بدون الحاجة للاشتراكات (Subscriptions).
* **مكتبات توليد الـ PDF والصور:**
  - `html-to-image`: لالتقاط نسخة نقطية (Rasterized) دقيقة للشهادة كما تظهر في الـ DOM، مع دعم دقة مضاعفة `pixelRatio: 2` للحفاظ على جودة الخطوط العربية المعقدة.
  - `jspdf`: لتكوين ملف الـ PDF وتحديد مقاسه (A4 أفقي) وحساب الإحداثيات لزراعة الروابط.
  - `qrcode`: لإنشاء الباركود كصورة Base64 وربطها بالبيانات.

---

## 📂 3. هيكلية المجلدات والملفات (Folder Structure)
يتبع المشروع نمط (Feature-Based Architecture)، وهو النمط الموصى به للمشاريع القابلة للتوسع (Scalable Projects):

```text
src/app/
├── core/                           # يحتوي على الأساسيات التي يبنى عليها التطبيق
│   ├── guards/                     # حراس التوجيه لمنع الوصول غير المصرح به
│   │   └── auth.guard.ts           # يتحقق من وجود Token
│   ├── interceptors/               # مراقبة وتعديل طلبات HTTP
│   │   └── auth.interceptor.ts     # يضيف الـ Bearer Token لمعظم الطلبات
│   ├── mock/                       # البيانات الثابتة والوهمية
│   │   └── dictionary.ts           # قاموس اللغات (عربي/إنجليزي)
│   ├── models/                     # واجهات البيانات (TypeScript Interfaces)
│   │   └── certificate.ts          # هيكل الشهادة
│   └── services/                   # الخدمات المشتركة (Singletons)
│       ├── auth.service.ts         # تسجيل الدخول وإدارة الـ Token
│       ├── certificate.service.ts  # تمرير بيانات الشهادة المنشأة
│       ├── language.service.ts     # إدارة اللغة الحالية باستخدام Signals
│       ├── loading.service.ts      # التحكم في شاشة التحميل العامة
│       └── verification.service.ts # التخاطب مع API التوثيق
├── features/                       # يحتوي على وحدات النظام المستقلة
│   ├── certificate-preview/        # معاينة وتصدير الشهادة
│   │   ├── components/             # القوالب الفرعية
│   │   │   ├── classic-certificate/
│   │   │   ├── elegant-certificate/
│   │   │   └── quran-certificate/  # لكل قالب HTML/TS خاص به
│   │   ├── certificate-preview.ts  # منطق تجميع الشهادة والـ PDF
│   │   └── certificate-preview.html
│   ├── certificate-verification/   # شاشة التوثيق العامة (Public)
│   │   ├── certificate-verification.ts
│   │   └── certificate-verification.html # تصميم الـ Dashboard
│   ├── create-certificate/         # شاشة إدخال بيانات الشهادة الجديدة
│   ├── dashboard/                  # لوحة التحكم الرئيسية (Layout)
│   ├── expired-courses/            # شاشة الدورات المنتهية
│   └── login/                      # شاشة تسجيل الدخول
├── app.routes.ts                   # ملف تعريف المسارات (Routing)
├── app.config.ts                   # إعدادات التطبيق الأساسية (Providers, Interceptors)
└── app.ts                          # نقطة الانطلاق الأساسية للتطبيق (Root Component)
```

---

## 🧠 4. إدارة الحالة باستخدام (Signals)
تم التخلي عن الأنماط القديمة، واستخدام `Signals` في جميع أرجاء المشروع لضمان التحديث التلقائي للواجهة (Reactivity):

- **التصريح:** `myState = signal<string>('initial')`
- **الحصول على القيمة:** `this.myState()` (في القالب أو الكود).
- **القيم المحسوبة (Computed):** `computed(() => ...)` تُستخدم لاستنتاج بيانات جديدة، مثلاً في `certificate-verification.ts`:
  ```typescript
  firstSessionDate = computed(() => {
    const data = this.courseData();
    // ترتيب الحصص وإرجاع أول تاريخ تلقائياً عند تحديث البيانات
  });
  ```
  هذا يضمن عدم تشغيل دوال ثقيلة داخل قالب الـ HTML.

---

## 🔄 5. التدفقات البرمجية التفصيلية (Detailed Workflows)

### 5.1. نظام المصادقة والحماية (Auth Flow)
**الهدف:** منع أي مستخدم غير مصرح له من الوصول للوحة التحكم أو إصدار شهادات.
- **التسجيل (Login):** يدخل المستخدم اسم المستخدم وكلمة المرور. يتصل `AuthService` بالخادم، وفي حال النجاح يتم استلام `Token` وحفظه في `localStorage` تحت المفتاح `accessToken`.
- **الحارس (AuthGuard):** أي محاولة لفتح مسار (مثل `/dashboard` أو `/certificates`) تمر عبر `auth.guard.ts`. إذا لم يكن هناك Token، يُعاد توجيه المستخدم لصفحة `/login`.
- **المعترض (AuthInterceptor):** عندما يقوم التطبيق بأي طلب API (عبر `HttpClient`)، يتدخل المعترض:
  1. يستثني طلبات تسجيل الدخول والتوثيق (Verification) لأنها عامة.
  2. يضيف `Authorization: Bearer <token>` للترويسة (Headers).
  3. يراقب أخطاء الردود (`HttpErrorResponse`)، وإذا كان الخطأ `401 Unauthorized` (انتهت الجلسة)، يقوم بمسح التوكن المخزن وتوجيه المستخدم لتسجيل الدخول.

### 5.2. إنشاء ومعاينة الشهادات (Certificate Flow)
**الهدف:** إدخال بيانات الشهادة واختيار تصميمها، ثم عرضها.
- في `create-certificate` يتم ملء نموذج بيانات الشهادة (Form).
- عند الضغط على "إنشاء"، يتم تمرير البيانات ككائن (Object) إلى `certificateService.setCertificate(data)`، ثم يتم الانتقال عبر الراوتر إلى صفحة `/certificates/preview`.
- في صفحة المعاينة، يتم قراءة البيانات من الخدمة.
- بناءً على الحقل `templateId` (كلاسيك، أنيق، قرآني)، يتم استخدام `Control Flow` الخاص بـ Angular (`@if`) لعرض المكون (Component) المناسب. تم استخدام `@defer (on immediate)` لتحميل مكونات الشهادات بشكل كسول (Lazy) لتقليل زمن التحميل الأولي.

### 5.3. محرك التصدير لملفات PDF والروابط التفاعلية (PDF Engine)
**الهدف:** تحويل الشهادة المعروضة في الشاشة لملف PDF ورقي، مع الاحتفاظ بفاعلية الرابط المخفي بداخلها.
**التحدي التقني:** أخذ لقطة شاشة (Screenshot) يحول كل شيء إلى صورة (Pixels)، وبالتالي تُفقد الروابط (Links).
**الحل الهندسي الموجود في `downloadCertificate()`:**
1. **التأخير للرسم (Paint Delay):** ينتظر النظام 500 ملي ثانية لضمان تحميل الخطوط والصور والـ QR Code تماماً.
2. **التقاط الصورة:** يستخدم `html-to-image` لالتقاط العنصر `#certificate-download-container` كـ Jpeg عالي الدقة.
3. **تكوين الـ PDF:** يتم إنشاء كائن `jsPDF` بمقاس A4 أفقي (`landscape`).
4. **حساب أبعاد الـ PDF:** يتم حساب العرض والطول بالتناسب مع صورة الـ HTML لضمان عدم تشوه الأبعاد (Aspect Ratio).
5. **إضافة الرابط التفاعلي (السر التقني):**
   - يتم البحث عن العنصر الذي يحمل المعرف `#pdf-verify-link` (وهو الحاوية التي تضم الباركود وزر التحقق).
   - باستخدام `getBoundingClientRect()`، يتم حساب المسافة بين هذا الزر وبين حافة الشهادة الأصلية، وتحويلها لنسب مئوية (`xPercent`, `yPercent`, `wPercent`, `hPercent`).
   - تُضرب هذه النسب في أبعاد الـ PDF الحقيقية لمعرفة موقع الزر تماماً داخل الصفحة.
   - يتم زراعة رابط غير مرئي `pdf.link(...)` فوق هذه المساحة المحددة داخل الـ PDF.
6. **التنزيل:** يتم حفظ الملف باسم الطالب.

### 5.4. نظام التوثيق العام (Verification Dashboard)
**الهدف:** عرض تفاصيل الشهادة بطريقة احترافية تعزز ثقة أولياء الأمور والجهات المعنية.
- **الوصول (Routing):** يتم عبر الرابط `/certificates/Verification/:id`.
- **جلب البيانات (Data Fetching):** يقرأ الـ `id` من الرابط، ويمرره إلى `VerificationService` للتحقق عبر الـ API.
- **التصميم الهندسي (UI/UX):** 
  - **Glassmorphism:** استخدام خلفيات شفافة مع تأثيرات ضبابية `backdrop-blur` لتبدو الواجهة عصرية.
  - **تأثيرات النبض (Animations):** تم بناء درع موثوقية يحمل الفئة `animate-ping` و دائرة شعاعية `animate-[spin_10s_linear_infinite]` ليعطي إيحاء بأن الصفحة حية وتفاعلية.
  - **Grid System:** تم تقسيم معلومات الكورس (معلم، مادة، مدة) في (Bento Grid) باستخدام Tailwind `grid-cols-1 sm:grid-cols-2`.
  - **معالجة التواريخ (Dates):** يتم تحويل التواريخ المعقدة إلى شكل مقروء عبر خوارزميات مخصصة (تحويل 0-6 إلى أيام الأسبوع، وحساب أول وأخر حصة).

---

## 🌐 6. نظام تعدد اللغات (i18n & Localization)
لضمان السرعة القصوى وعدم الاعتماد على مكتبات خارجية مثل `ngx-translate`:
- **ملف القاموس (`dictionary.ts`):** 
  ملف ثابت (Const) يحتوي على مفاتيح النصوص لكل من اللغتين (`ar` و `en`). يتمتع بحماية نوع (Type Safety) صارمة في TypeScript.
- **خدمة اللغة (`LanguageService`):**
  تحمل الـ State الخاصة باللغة كـ Signal (`currentLanguage`). عند الاستدعاء `setLanguage('en')`، تتغير الـ Signal.
- **التطبيق في الواجهات:**
  - يتم جلب النصوص من القاموس عن طريق استدعاء مفتاح محدد بناءً على اللغة الحالية. مثال: `texts['verificationTitle']`.
  - **الاتجاه (Direction):** يتم حساب الـ `dir` ديناميكياً (rtl أو ltr) وربطه بـ `[attr.dir]` في الغلاف الخارجي للمكونات. بفضل Tailwind، يتم استخدام الـ Logical Properties (مثل `start-0` و `end-4`) بحيث يتقلب التصميم بالكامل كالمرايا عند تغيير اللغة تلقائياً بدون أي سطر CSS إضافي.

---

## 🧰 7. الخدمات الأساسية (Core Services)

1. **`AuthService`**: يدير تسجيل الدخول، التحقق من حالة الجلسة، والاحتفاظ بالـ Token في الـ Local Storage.
2. **`LanguageService`**: يوفر الـ Signal الذي يحمل اللغة الحالية لتشغيل الـ Reactivity في جميع الصفحات.
3. **`CertificateService`**: خدمة لتمرير البيانات في الذاكرة (In-Memory) بين صفحة "الإنشاء" وصفحة "المعاينة".
4. **`VerificationService`**: مسؤولة عن جلب حالة الشهادة من الـ API العام، تقوم بمعالجة الرد وبناء واجهة `VerificationData` لاستخدامها في صفحة التوثيق.
5. **`LoadingService`**: خدمة تتحكم في إظهار وإخفاء شاشة التحميل (Spinner) لتعطيل واجهة المستخدم أثناء جلب البيانات أو أثناء معالجة ملفات الـ PDF الثقيلة لمنع الضغط المتكرر.

---

## 📊 8. واجهات البيانات (Models & Interfaces)
تم تعريف واجهات صارمة لضمان صحة البيانات المتبادلة بين العميل والخادم:
- `Certificate`: واجهة تصف الشهادة (اسم الطالب، الكورس، تاريخ الإصدار، قالب الشهادة، ورقم التوثيق `sspId`).
- `VerificationData`: واجهة معقدة تتضمن تفاصيل السجل الأكاديمي (`record`) وتفاصيل الكورس وتوزيع الحصص، وأسماء المدرسين والمشرفين.

---
**تم كتابة هذا التوثيق ليكون مرجعاً معمارياً للمطورين القادمين، يضمن الفهم الكامل لآلية عمل النظام وكيفية التعديل عليه باحترافية.**
