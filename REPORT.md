# توثيق مشروع "أكاديمية كنوز" (Knoz Academy)

## 1. نظرة عامة (Overview)
مشروع "أكاديمية كنوز" هو نظام إدارة شهادات للطلاب، يعتمد على بنية **Full-Stack** تم تصميمها حديثاً لتشمل كلاً من المتصفح والخادم (Server-Side Rendering & API Proxy) لتعزيز الحماية والأداء.
يتيح النظام إنشاء، إدارة، وعرض شهادات إتمام الدورات للطلاب، مع توفير خاصية التحقق من الشهادة (Verification) عبر مسح رمز الاستجابة السريعة (QR Code).

---

## 2. التقنيات المستخدمة (Technology Stack)

### الواجهة الأمامية (Frontend)
- **إطار العمل:** Angular 21 (بأحدث معايير المكونات المستقلة Standalone Components).
- **التصميم وتنسيق الواجهات:** Tailwind CSS (نسخة v4) لبناء واجهات متجاوبة (Responsive) وتصميمات حديثة مثل (Glassmorphism, Bento Grid).
- **مكتبات إضافية:** 
  - `html-to-image` و `jspdf`: لتصدير الشهادات بصيغة PDF عالية الجودة كصورة.
  - `qrcode`: لتوليد رمز الاستجابة السريعة للشهادات للتحقق منها.

### الواجهة الخلفية والخادم (Backend & SSR)
- **بيئة التشغيل:** Node.js.
- **الخادم (Proxy & SSR):** Express.js.
- **التصيير من جهة الخادم (SSR):** Angular SSR لتهيئة الصفحة على السيرفر قبل إرسالها للمتصفح (يعالج مشاكل SEO ويزيد من سرعة التحميل).
- **الأمان:** 
  - تم بناء "Proxy" داخلي (`src/server.ts`) يتعامل مع الـ API الخارجي.
  - تم إخفاء بيانات تسجيل الدخول (Username/Password) وبيانات التوثيق الحساسة من الكود، حيث تتواجد فقط على السيرفر (بيئة `Node.js`) لمنع زوار الموقع من رؤيتها عبر "أدوات المطور" (F12).

---

## 3. هيكل المشروع الأساسي (Directory Structure)

```text
/src
 ├── app/
 │   ├── core/
 │   │   ├── guards/          # حماية مسارات الروابط (AuthGuard)
 │   │   ├── interceptors/    # اعتراض الطلبات لإضافة توكن الأمان (auth.interceptor.ts)
 │   │   ├── mock/            # القاموس والنصوص (dictionary.ts)
 │   │   ├── models/          # تعريفات الأنواع والبيانات (certificate.ts)
 │   │   └── services/        # الخدمات المركزية (مثل: AuthService, CertificateService)
 │   │
 │   └── features/
 │       ├── login/                    # شاشة تسجيل الدخول
 │       ├── dashboard/                # لوحة التحكم الرئيسية
 │       ├── create-certificate/       # شاشة إدخال بيانات الشهادة الجديدة
 │       ├── certificate-preview/      # معاينة الشهادة وتحميلها وطباعتها
 │       ├── certificate-verification/ # شاشة فحص الشهادات للعامة
 │       ├── expired-courses/          # إدارة الدورات المنتهية
 │       └── settings/                 # شاشة الإعدادات
 │
 ├── environments/    # إعدادات البيئة (مخفية عن GitHub - تحتوي على الكلمات السرية)
 ├── server.ts        # خادم Express و Proxy لحماية البيانات
 └── main.server.ts   # نقطة الدخول لتصيير Angular على السيرفر (SSR)
```

---

## 4. تدفق العمليات (Workflows & Flows)

### أ. دورة تسجيل الدخول (Authentication Flow)
1. يفتح المستخدم شاشة `login`.
2. عند إدخال البيانات، يتم استدعاء `AuthService.login()`.
3. لا يتصل النظام بالـ API الخارجي مباشرة! بل يرسل الطلب إلى الخادم الداخلي `/api/login` (في `server.ts`).
4. الخادم الداخلي (`server.ts`) يسحب الكلمات السرية الآمنة من `environments` ويتصل بالـ API الخارجي.
5. في حال نجاح الدخول، يعود "Token" للواجهة الأمامية ويتم حفظه في `localStorage` (بشكل محمي عبر SSR).
6. يتم إعادة توجيه المستخدم إلى شاشة لوحة التحكم `dashboard`.

### ب. إنشاء وتصدير الشهادة (Certificate Generation Flow)
1. يدخل المستخدم إلى `create-certificate` ويقوم بتعبئة: اسم الطالب، اسم الدورة، تاريخ الإصدار.
2. يتم قراءة "القالب الافتراضي" و"اسم المُوقّع الافتراضي" من خدمة الإعدادات `SettingsService`.
3. عند الضغط على حفظ، يتم تخزين الشهادة في `localStorage` عبر `CertificateService`.
4. يتم الانتقال لشاشة المعاينة `certificate-preview`.
5. تعرض الشاشة القالب المحدد مسبقاً (كلاسيكي، أنيق، قرآني).
6. عند ضغط **تحميل (Download)**: تقوم مكتبة `html-to-image` بتصوير القالب، ثم تقوم `jspdf` بتحويل الصورة إلى ملف PDF مع إضافة رابط تفاعلي مخفي فوق رمز الـ QR لتسهيل التحقق الرقمي.

### ج. شاشة التحقق (Verification Flow)
1. يقوم الزائر بمسح الـ QR Code عبر كاميرا هاتفه.
2. يتم توجيه الزائر إلى رابط يتبع النمط: `/certificates/Verification/:id`.
3. تعمل شاشة `certificate-verification` على استخراج الـ `id` من الرابط، وتطلب البيانات من `/api/verify` (عبر Proxy الداخلي لحماية التوكن).
4. إذا كان الـ ID صحيحاً، يتم عرض واجهة أنيقة (بطاقة تعريفية) تحتوي على بيانات الكورس والطالب وموثوقية الشهادة.
5. إذا كان الرابط خطأ، يتم عرض رسالة خطأ واضحة.

### د. إعدادات النظام (Settings Flow)
1. يوفر النظام شاشة إعدادات `settings` لتخصيص سلوك النظام.
2. تتيح للمستخدم:
   - تغيير لغة الواجهة (عربي/English) فورياً.
   - تحديد "القالب الافتراضي" (Default Template) لكي لا يحتاج لتحديده كل مرة في شاشة الإنشاء.
   - تحديد "اسم المُوقّع الافتراضي" لطباعته على الشهادات.
3. يتم حفظ هذه البيانات داخلياً في المتصفح `localStorage` عبر `SettingsService` بحيث تبقى محفوظة للمستخدم في الزيارات القادمة.

---

## 5. ملاحظات أمنية وهندسية هامة

1. **دعم SSR (Server-Side Rendering):** 
   تم تصميم النظام ليدعم بيئة السيرفر بنجاح، وتطلب ذلك معالجة الأخطاء الناتجة عن محاولة قراءة `localStorage` و `document` و `window` أثناء البناء على السيرفر. جميع الأكواد التي تستخدم هذه الميزات تم تغليفها بشرط:
   ```typescript
   if (isPlatformBrowser(this.platformId)) { ... }
   ```
2. **إخفاء الكلمات السرية (Proxy Pattern):**
   لحل مشكلة تسريب بيانات تسجيل الدخول الثابتة في الـ Frontend، تم نقلها إلى السيرفر الخاص بـ Node.js، بحيث يتعذر تماماً على أي زائر يستخدم (F12 / DevTools) رؤية الكلمات السرية المستخدمة للتواصل مع الـ API الخارجي.

3. **حماية مستودع GitHub:**
   تم إضافة مجلد `src/environments` إلى ملف `.gitignore`، لضمان عدم رفع أي مفاتيح أو بيانات دخول حساسة إلى المستودع العام.

---
**نهاية التقرير.**
